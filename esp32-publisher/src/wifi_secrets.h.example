#pragma once

#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

// Your computer's LAN IP (e.g. `ipconfig getifaddr en0` on macOS) and the
// manager's TCP port (must match TCP_PORT in src/manager.cpp).
#define MANAGER_HOST "192.168.1.100"
#define MANAGER_PORT 7001
